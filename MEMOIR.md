---
name:             "MEMOIR.md"
description:      "台灣機車多點配送路徑規劃系統 — 開發回顧與設計決策紀錄"
created_date:     "2026/05/25 18:00:00"
modified_date:    "2026/05/26 12:15:00"
project_version:  "1.1.0"
document_version: "1.0.1"
agent_sign:       ['human/justin', 'antigravity/gemini-cli']
---

# MEMOIR — 開發回顧文件

## 台灣機車多點配送路徑規劃系統 (MyOSM)

> 本文件記錄此專案從無到有的完整開發歷程、關鍵設計決策與技術挑戰，供日後維護與版本演進參考。

---

## 1. 專案起源

### 1.1 問題背景

台灣外送市場高度依賴機車，但現有開源路徑規劃工具（如 Google Maps、OSRM 預設設定）均以汽車路權為基礎，未針對機車做任何調整，導致：

- **路線不符現實**：規劃出的路線包含機車不得行駛的國道／高速公路。
- **時間估算失準**：市區時速參數偏高，外送平台的到達時間預測誤差大。
- **無法離線使用**：商業 API 依賴雲端，成本高且有資料隱私疑慮。

### 1.2 核心訴求

以「**完全本地化、機車專屬、多點 TSP 最佳化**」為三大設計目標，建立一套可在個人電腦上運行的完整配送路線規劃工具。

---

## 2. 技術選型決策

### 2.1 路由引擎：為何選擇 OSRM？

| 方案 | 優點 | 缺點 | 決定 |
|------|------|------|------|
| Google Maps API | 準確、即開即用 | 付費、無法客製路權 | ❌ |
| Valhalla | 支援多種交通工具 | 設定複雜、資源耗用高 | ❌ |
| GraphHopper | Java 生態成熟 | JVM 佔用記憶體大 | ❌ |
| **OSRM** | **輕量、Lua 客製彈性大、Docker 部署簡單** | 需自行編譯圖資 | ✅ |

OSRM 的 `Lua profile` 機制讓我們能精確控制每種道路類型的路權與速限，且 MLD 演算法對 Trip（TSP）API 有優異的效能表現。

### 2.2 演算法：MLD vs CH

OSRM 支援兩種演算法：

- **CH（Contraction Hierarchies）**：適合兩點最短路徑，速度極快。
- **MLD（Multi-Level Dijkstra）**：適合 Trip API（TSP 多站最佳化），支援動態更新。

由於核心需求是多站配送（TSP），選擇 **MLD** 進行 `partition + customize` 編譯流程。

### 2.3 前端技術：為何不用 React/Vue？

考量到部署環境的簡潔性，以及希望「**零 npm install**」即可啟動，選擇：

- **純 HTML + Vanilla JS**：無框架依賴，修改直覺。
- **Leaflet.js（CDN）**：成熟的開源地圖套件，輕量且文件完整。
- **原生 Node.js 伺服器**：用 `http`、`fs`、`path` 模組實現靜態檔案服務，無任何 npm 套件。

---

## 3. 關鍵技術挑戰與解決方案

### 3.1 Leaflet 載入失敗（黑色地圖）

**問題**：初始版本底圖顯示全黑，路線無法繪製。

**根因分析**：
1. CDN 連結使用了已棄用的舊版 URL。
2. `motorcycle.lua` 中 `motorway` 封鎖導致某些路線無法生成，前端未處理空回應。

**解決方案**：
- 更新 Leaflet CDN 至 v1.9.4 穩定版。
- 新增 OSRM API 回應的 `code !== "Ok"` 錯誤處理邏輯。
- 提供地圖主題切換（Dark / Voyager / OSM），讓使用者可選擇較亮的底圖以確認路線。

### 3.2 `roundtrip` 參數邏輯錯誤

**問題**：使用者切換「送完配送點返回起點」開關無效，路線永遠閉合。

**根因分析**：
OSRM Trip API 的 `roundtrip` 參數預設為 `true`，且優先級高於 `destination` 參數。前端雖設定 `destination=last`，但未明確傳入 `roundtrip=false`，OSRM 仍計算閉合路徑。

**解決方案**：
動態將 `roundtrip=${isRoundTrip}` 加入 API 請求，使開關狀態直接控制 OSRM 行為。

### 3.3 圖資路徑管理

**問題**：初期 27 個 OSRM 編譯輸出檔案散落於專案根目錄，與程式碼混雜，不易管理。

**解決方案**：
建立 `osrm_data/` 專用目錄，統一存放原始 `taiwan-latest.osm.pbf` 及所有編譯輸出，並更新 `HIST_GEMINI.md` 中所有 Docker 指令的路徑引用。

---

## 4. 設計決策紀錄

### 4.1 UI 設計風格

選擇**玻璃擬態（Glassmorphism）深色主題**，理由：

1. **配送場景適用**：深色背景在夜間或日間戶外環境下對眼睛友善。
2. **地圖對比**：深色控制面板與地圖底圖形成良好視覺分離。
3. **科技感**：符合路線規劃工具的「科技導航」定位。

技術實作：`backdrop-filter: blur(20px)`、`rgba` 半透明背景、`box-shadow` 輝光效果。

### 4.2 路線顏色系統

提供兩種描邊策略：

| 策略 | 觸發條件 | 效果 |
|------|----------|------|
| **Neon Glow** | 深色底圖（Dark Mode） | 霓虹外發光，高科技感 |
| **Road Halo** | 亮色底圖（Voyager/OSM） | 白色外框描邊，提升可讀性 |

底圖切換時自動切換描邊策略，維持路線在任何底圖上的清晰度。

### 4.3 機車路權設定哲學

`motorcycle.lua` 的核心原則是「**保守優於激進**」：

- 速限寧可偏低，使時間估算**略為高估**而非低估（避免外送遲到）。
- 遇到存取權限不明確的道路類型，優先**封鎖**而非允許。
- 台灣特有的「省道實際車速」遠低於法定速限，設定時以**實際平均速度**為準。

---

## 5. 開發協作歷程摘要

本專案透過 AI 輔助開發完成，完整的人機對話紀錄存於 [`HIST_GEMINI.md`](./HIST_GEMINI.md)。

### 開發階段時序

| 階段 | 完成時間 | 主要成果 |
|------|----------|----------|
| 環境建置 | 2026/05/25 上午 | Docker OSRM + motorcycle.lua 編譯成功 |
| 前端初版 | 2026/05/25 下午 | Leaflet 地圖 + OSRM 串接 |
| UI 精緻化 | 2026/05/25 下午 | Glassmorphism 面板、主題切換、Halo 描邊 |
| Bug 修復 | 2026/05/25 傍晚 | roundtrip 參數修正 |
| 目錄整理 | 2026/05/25 傍晚 | osrm_data/ 建立，根目錄清爽化 |
| 文件撰寫 | 2026/05/25 傍晚 | README、CHANGELOG、SPEC、MEMOIR 建立 |
| 前端重構 | 2026/05/26 中午 | 模組化拆分、Leaflet 本地化、設定檔系統 |

---

## 6. 未來可能的改進方向

| 優先度 | 項目 | 說明 |
|--------|------|------|
| 高 | 離線底圖支援 | 整合 mbtiles + tileserver-gl，讓底圖也能離線使用 |
| 高 | 重型機車模式 | 新增 `motorcycle_heavy.lua` 允許部分快速道路 |
| 中 | 時間窗口限制 | 支援各配送點的時間區間（Time Window TSP） |
| 中 | 批次匯入站點 | 支援 CSV / JSON 格式批次匯入配送地址 |
| 低 | 行動裝置 PWA | 將前端打包為 PWA，支援手機操作 |
| 低 | 圖資自動更新 | 定期自動從 geofabrik 下載並重新編譯最新圖資 |

---

## 7. 維護注意事項

1. **更新圖資**：每 3～6 個月建議更新一次 `taiwan-latest.osm.pbf` 以反映最新道路變化，需重新執行 extract → partition → customize 三步驟。
2. **修改路權**：調整 `motorcycle.lua` 後必須重新完整編譯（三步驟），僅重啟容器無效。
3. **Docker 重啟**：每次重開機後需手動執行 Docker 啟動指令（見 `README.md`），目前未設定 `--restart=always`。
